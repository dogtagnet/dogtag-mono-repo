# Pet-Owner (holder) wallet - environment.
#
# The wallet has no backend and currently requires no environment-specific endpoints.
# src/lib/config.ts supplies the guarded ROAX RPC default; Settings can persist another peer only
# for the same bundled chain. There is no user-configurable provider directory/indexer endpoint.
