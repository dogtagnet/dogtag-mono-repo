# Pet-Owner (holder) wallet - environment.
#
# The wallet has no backend and currently requires no environment-specific endpoints. Credential
# validity is read directly from the fixed ROAX network configured in src/lib/config.ts.
