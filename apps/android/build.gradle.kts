plugins {
    // AGP 9.x has built-in Kotlin support (no separate kotlin.android plugin needed).
    id("com.android.application") version "9.2.1" apply false
    id("org.jetbrains.kotlin.plugin.compose") version "2.3.21" apply false
}
