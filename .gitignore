# node / pnpm
node_modules/
dist/
*.tsbuildinfo
.pnpm-store/

# rust
target/

# foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# circuits — build artifacts (keep source + pinned vectors)
circuits/build/
circuits/*.zkey
circuits/*.r1cs
circuits/*.wasm
circuits/*.sym
circuits/ptau/*.ptau
circuits/log_*.txt
# DEV/throwaway Groth16 verifiers (regenerated by setup-consent.sh) — NEVER deploy these.
circuits/*.dev.sol

# env / secrets
.env
.env.*
!.env.example
*.keystore
*.age

# os
.DS_Store
.demo/

# agent memory symlink (AGENTS.md is the committed standard)
CLAUDE.md

# S-12 cutover rehearsal: the PID of the anvil scripts/rehearse-cutover.sh started, so a
# stranded fork can be killed BY PID rather than by name. Runtime state, never source.
.anvil-rehearsal.pid
